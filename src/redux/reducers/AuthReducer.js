import { LOGIN } from '../actions/types';

const initState = {
    login: false
};

export default function todo(state = initState, action) {
    switch (action.type) {

        default:
            return state
    }
}